apiVersion: 1
datasources:
  - name: Elasticsearch
    uid: elastic-wm
    type: elasticsearch
    access: proxy
    url: http://elasticsearch:9200
    jsonData:
      index: wm-metrics
      timeField: "@timestamp"
